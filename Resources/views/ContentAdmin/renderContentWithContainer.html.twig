<{{ container_html_tag }} id="{{ container_id }}" {{ container_html_attributes | raw }}>
{% render bundle_name ~ ':' ~ controller_name ~ ':renderContent' with {'content': content, 'render_template': render_template, 'custom_params': custom_params} %}
</{{ container_html_tag }}>
