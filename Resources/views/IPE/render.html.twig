{% if params.editor_roles is empty or is_granted(params.editor_roles) %}
<{{ params.container_html_tag }}  {{ params.container_html_attributes | raw }} data-ipe-hash="{{ data_ipe_hash }}" data-hah-action="load-modal" data-hah-url="{{ path('_ipe_action', {'action': 'edit', 'ipe_hash': data_ipe_hash}) }}" data-hah-id-selector="#hah-modal" data-hah-block-message="{{ 'controller.ajax_loading'|ipe_trans }}">
{% endif %}

  {{ include(render_template) }}

{% if params.editor_roles is empty or is_granted(params.editor_roles) %}
</{{ params.container_html_tag }}>
{% endif %}